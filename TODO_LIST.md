# 当前任务计划

## 配置导入导出改为数据库真值

- [ ] 后端测试：新增最小测试覆盖 Provider 导出后再导入能写入 SQLite。
  - 验证方式：运行定向 Rust 测试。
  - 当前状态：红灯阶段已确认旧签名无法接收数据库；实现后测试被本机 MSVC `cl.exe` D8037 临时文件错误阻塞，未完成绿灯验证。
- [ ] 后端实现：`export_config` / `import_config` / `export_providers_config` / `import_providers_config` 全部改为读取和写入 SQLite，不再写旧 JSON 文件。
  - 验证方式：定向 Rust 测试通过。
  - 当前状态：代码已实现，等待 Rust 工具链恢复后验证。
- [ ] 命令接线：`utility_commands.rs` 为导入导出命令注入 `AppState`。
  - 验证方式：`cargo check --manifest-path src-tauri/Cargo.toml` 通过。
  - 当前状态：代码已实现；`cargo check` 被本机 MSVC / Windows SDK 环境阻塞。
- [ ] 前端刷新：导入后刷新当前配置状态，避免导入成功后 UI 仍显示旧缓存。
  - 验证方式：`npm run build` 通过。
  - 当前状态：代码已实现；当前 Node 运行项目脚本时触发 `ncrypto::CSPRNG` 断言，构建未完成。
- [ ] 全量验证：完成前运行编译检查。
  - 验证方式：`npm run build` 与 `cargo check --manifest-path src-tauri/Cargo.toml` 通过。
  - 当前状态：`cargo fmt --check` 通过；编译验证受本机工具链环境阻塞。

## 工作空间模块迁移规划

- [ ] 方案确认：确认 `docs/superpowers/plans/2026-04-29-workspace-module-migration.md` 作为后续实施蓝图。
  - 验证方式：用户明确确认第一批实施范围。
  - 当前状态：已确认。
- [ ] 第一批：Workspace CRUD + 会话关联 + 静态 registry。
  - 验证方式：每个小功能完成后分别运行对应 Rust 单测、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`。
  - 当前状态：Task 1 类型层已完成；Task 2 路径归一化工具已完成；Task 3 数据库 schema 已完成，均通过只读 spec / quality 复核。
  - 已验证：`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过；TypeScript diagnostics 与 Vite build 通过。
  - 环境阻塞：`cargo check --manifest-path src-tauri/Cargo.toml` 被本机 MSVC `cl.exe` 缺失阻塞，尚不能作为代码通过证据。
  - 当前进度：Task 4 Workspace DAO 已完成并通过只读 spec / quality 复核；`cargo test --manifest-path src-tauri/Cargo.toml database::dao::workspaces::tests -- --nocapture` 已确认无本项目 Rust 编译错误，但在 `link.exe` 阶段阻塞，未执行到测试运行。
  - 当前进度：Task 5 Workspace service 已完成并通过只读 spec / quality 回归复核；已修复 quality 复核提出的 4 个阻塞点：`touch_workspace` 改为专用 DAO 更新、`create/import/resolve` 使用 canonical path 作为身份、`origin_url` 解析失败时不落库、`origin_url` 默认移除全部 query / fragment。
  - Task 5 当前验证：`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过；TypeScript diagnostics 通过；Vite build 通过；`cargo test --manifest-path src-tauri/Cargo.toml services::workspace_service::tests -- --nocapture` 与 `cargo test --manifest-path src-tauri/Cargo.toml database::dao::workspaces::tests -- --nocapture` 已确认无本项目 Rust 编译错误，但在 `link.exe` 阶段阻塞，未执行到测试运行。
  - 当前进度：Task 6 Tauri workspace commands 已完成并通过只读 spec / quality 复核；实现 workspace CRUD、import、touch、binding commands；删除不存在 id 采用明确错误。
  - Task 6 当前验证：`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过；`git diff --check` 通过；TypeScript diagnostics 通过；Vite build 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 被本机 `cl.exe` / `link.exe` 环境阻塞在第三方 build scripts 阶段。
  - 当前进度：Task 7 前端 workspace service / store 已完成并通过只读 spec / quality 回归复核；实现 `workspaceService.ts` invoke 封装与 `useWorkspaceStore.ts` 列表、选中项、CRUD、导入、touch 状态管理；已处理复核非阻塞建议中的 type-only import 与 mutation 后刷新失败可感知；暂未接入 UI。
  - Task 7 当前验证：TypeScript diagnostics 通过；Vite build 通过；`git diff --check` 通过；`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过。
  - 当前进度：Task 8 Workspace UI 最小 CRUD 已完成并通过只读 spec / quality 复核；实现 `WorkspaceList`、`WorkspaceFormModal`、`WorkspaceActions`，`WorkspacesPage` 第一列优先展示持久化 workspace，保留历史项目导入 / 浏览入口，选中 workspace 后复用现有会话面板。
  - Task 8 当前验证：TypeScript diagnostics 通过；Vite build 通过；workspace i18n 静态对照通过；`git diff --cached --check` 通过。说明：默认 Node 22/24 仍会触发 `ncrypto::CSPRNG` 断言，验证使用 `C:\Users\Administrator\AppData\Local\nvm\v20.11.1\node.exe` 直接执行 `tsc --noEmit` 与 `vite build`。
  - 当前进度：Task 9 Workspace 与现有 session_manager 关联已完成；新增 `WorkspaceSessionPanel`，内部按 `rootPath` 调用 `list_sessions({ projectPath })`，保留 provider filter；`WorkspacesPage` 右侧消息详情继续复用 `get_unified_session_messages(providerId, sourcePath)`，恢复会话继续复用 `launch_resume_session`。
  - Task 9 当前验证：TypeScript diagnostics 通过；Vite build 通过；`git diff --cached --check` 通过。已处理复核阻塞点：新增组件纳入 Git、消息详情加载增加 request id 防旧请求覆盖、`resumeCommand` 为空时不再展示 / 复制前端伪造命令。说明：验证仍使用 `C:\Users\Administrator\AppData\Local\nvm\v20.11.1\node.exe` 直接执行 `tsc --noEmit` 与 `vite build`。
  - 当前进度：Task 10 Adapter registry 静态化已完成；新增 Rust `AdapterRegistry` / `AppIntegration` / `ModelAdapter` 类型、只读 `adapter_registry_service`、`get_adapter_registry` Tauri command，以及前端 `adapter.ts`、`adapterRegistryService.ts`、`useAdapterRegistryStore.ts`。当前 registry 仅静态集中 Claude / Codex / Gemini 固定事实，未接入核心 UI，不改变现有 provider / session / MCP 行为。
  - Task 10 当前验证：`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过；TypeScript diagnostics 通过；Vite build 通过；`git diff --cached --check` 通过。已新增 Rust 单测覆盖 registry 三项和 resume template；`cargo test --manifest-path src-tauri/Cargo.toml services::adapter_registry_service::tests -- --nocapture` 编译到本项目后仍被本机 `link.exe` 阶段阻塞；`cargo check --manifest-path src-tauri/Cargo.toml` 仍被本机 `cl.exe` / `link.exe` 环境阻塞在第三方 build scripts 阶段。
  - 当前进度：Task 11 Provider / model workspace 默认值已完成；新增 `WorkspaceBindingsPanel`，选中持久化 workspace 后可设置默认 app 和默认 provider；provider 列表复用 `useProviderStore` 并按 app 类型过滤；保存链路通过 `useWorkspaceStore.updateWorkspaceDefaults` 只写 `defaultAppType` 和 `defaultProviderId`，不复制 provider 密钥。
  - Task 11 当前验证：TypeScript diagnostics 通过；Vite build 通过；workspace i18n 静态对照通过；`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过；`git diff --check` 通过。说明：后端字段和 `update_workspace` 命令已在前序任务支持 nullable update，本任务未新增后端数据写入路径。
  - 当前进度：Task 12 Workspace capability binding 兼容视图已完成；`WorkspaceBindingsPanel` 已展示 workspace 级 MCP override，支持 `inherit / enabled / disabled` 三态；写入路径只调用 `set_workspace_binding` / `delete_workspace_binding`，不调用旧 `toggle_mcp_app`，不修改 `mcp_servers.enabled_claude/enabled_codex/enabled_gemini`；`workspace_service` 新增 effective MCP 纯合并逻辑，按 app 全局启用状态叠加 workspace override。
  - Task 12 当前验证：TypeScript diagnostics 通过；Vite build 通过；workspace i18n 静态对照通过；`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过；`git diff --check` 通过。新增 Rust 单测覆盖 MCP inherit / override enable / override disable；`cargo test --manifest-path src-tauri/Cargo.toml services::workspace_service::tests::build_effective_workspace_mcp_servers_merges_inherit_enable_and_disable -- --nocapture` 编译到本项目后仍被本机 `link.exe` 阶段阻塞，未执行到测试断言。
  - 当前进度：Task 13 CodexBridge 接口层已完成；新增 Rust conversation 类型、CodexBridge service stub、Tauri commands，以及前端 `conversation.ts` / `codexBridgeService.ts` invoke 封装。所有 CodexBridge command 当前都返回明确 `CodexBridge <operation> is not implemented`，不启动外部 Codex CLI、不接 UI、不展示假聊天入口。
  - Task 13 当前验证：TypeScript diagnostics 通过；Vite build 通过；`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过。新增 Rust 单测覆盖 conversation camelCase 序列化、未知字段拒绝、stub not implemented 错误；`cargo test` 与 `cargo check --manifest-path src-tauri/Cargo.toml` 仍被本机 MSVC / `cl.exe` / `link.exe` 环境阻塞，未执行到测试断言。
  - 当前进度：Task 14 Codex config / model 只读集成已完成；`codex_config_read` 支持只读解析 `$CODEX_HOME` 或用户目录 `.codex` 下的 `config.toml`，只返回 model、provider、approval、sandbox 和配置路径状态等非敏感摘要；Codex home 不存在时返回可解释错误，`config.toml` 不存在时返回默认空摘要。`codex_model_list` 只从 SQLite provider 默认模型字段派生候选，不联网、不返回 API key。
  - Task 14 当前验证：TypeScript diagnostics 通过；Vite build 通过；workspace i18n 静态对照通过；`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过；`git diff --check` 通过；新增 Rust 单测覆盖 config 安全摘要和 provider 默认模型派生，但 `cargo test --manifest-path src-tauri/Cargo.toml services::codex_bridge_service::tests::parse_codex_config_summary_only_returns_safe_fields -- --nocapture` 仍被本机 `link.exe` 阶段阻塞，未执行到测试断言。
  - 当前进度：Task 15 Conversation event reducer 前端已完成；新增 `useConversationStore`，提供纯 reducer、runtime state、snapshot 加载、事件批量应用、approval resolve、thread interrupt 和 reset；新增 `ConversationPanel`、`MessageItem`、`ApprovalCard` 三个独立展示组件。该阶段只消费模拟 / 外部传入事件，不调用 `codexBridgeService`、不启动真实 Codex CLI。已处理质量复核提出的边界：interrupt / failed 会收敛 running item，`approval_resolved` 增加显式 `approvalId`，error 按 thread 隔离，completed item 后缺 `itemId` 的 delta 会 raw fallback。
  - Task 15 当前验证：TypeScript diagnostics 通过；Vite build 通过；`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过；`git diff --check` 通过；增强 reducer 模拟通过，覆盖正常流、interrupt 清理 approval / streaming、malformed delta raw fallback、thread 级 error 隔离；并行规格复核未发现阻塞问题。Rust conversation 定向测试仍被本机 `link.exe` 阶段阻塞，未执行到测试断言。
  - 当前进度：Task 16 Chat UI 最小可用已完成；`WorkspacesPage` 选中持久化 workspace 后提供 `Overview / Sessions / Chat / Bindings` tabs，`Sessions` 继续复用现有历史会话浏览，`Chat` 接入 `ConversationPanel` 展示消息流和输入框，`Bindings` 承载默认 app / provider 和 MCP override 设置。Chat 面板注入的 `ThreadStartInput.cwd` 来自 `workspace.rootPath`，未传入 `onSubmitPrompt`，因此不显示未实现发送按钮，也不调用真实 Codex CLI。
  - Task 16 当前验证：TypeScript diagnostics 通过；Vite build 通过；`workspaces` / `conversation` i18n 静态对照通过；`git diff --check` 通过；代码检索确认 `WorkspacesPage` / `ConversationPanel` 未新增 `codex_thread_start`、`startCodexThread` 或 `codexBridgeService` 调用。全量 i18n 递归对照发现既有跨命名空间差异，但本任务相关命名空间对齐。
  - 当前进度：Task 17.1 / 17.2 Codex executable 发现与 thread start/read 已完成；`codex_thread_start` 会校验 workspace cwd 存在且为绝对目录，优先使用 `metadata.codexExecutablePath` / `metadata.codexExecutable` 配置的本地 executable，其次从 PATH 查找 `codex` 候选；不会执行远程脚本，也不会返回 token / API key / auth / base_url 等敏感信息；创建成功后 thread 保存在内存 runtime，`codex_thread_read` 可读取空 snapshot。`turn_start`、`turn_interrupt`、`thread_resume` 仍保持明确 not implemented，等待 Task 17.3+。
  - Task 17.1 / 17.2 当前验证：已按 TDD 先确认新测试红灯，再实现；`cargo test --manifest-path src-tauri/Cargo.toml services::codex_bridge_service::tests -- --nocapture` 通过 6 个测试；`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过；TypeScript diagnostics 通过；Vite build 通过；`git diff --check` 与 `git diff --cached --check` 通过。说明：Vite 使用 Node 20.11.1 构建成功但仍提示低于推荐版本。
  - 当前进度：Task 17.3 Codex turn start 最小流式事件转发已完成；`codex_turn_start` 注入 `AppHandle`，后台 worker 执行本地 `codex exec --json -C <cwd> --skip-git-repo-check -o <temp>`，根据 stdout JSONL 尝试提取 assistant delta，并通过 `codex://conversation-event` 向前端发送 `turn_started`、`item_completed`、`item_started`、`item_delta`、`item_completed`、`turn_completed` / `turn_failed`。前端 `WorkspacesPage` 注册事件 listener，Chat tab 首次发送时创建 thread、读取 snapshot，再启动 turn；不接 approval，不注入 MCP，不使用 `--dangerously-bypass-approvals-and-sandbox`。
  - Task 17.3 当前验证：`cargo test --manifest-path src-tauri/Cargo.toml services::codex_bridge_service::tests -- --nocapture` 通过 10 个测试；`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过；TypeScript diagnostics 通过；Vite build 通过；`git diff --check` 与 `git diff --cached --check` 通过。`codex exec --help` 已确认当前 CLI 支持所需参数；一次 `codex exec` smoke 进程成功退出，但终端未捕获到可用输出文本，因此真实 UI 对话效果仍需要 Tauri dev 手动回归。
  - 当前进度：Task 17.4 Codex turn interrupt 已完成；runtime 保存当前 turn 的 Codex child process，`codex_turn_interrupt` 注入 `AppHandle` 后可 kill 当前进程、标记 thread 为 `interrupted`、turn 为 `interrupted`、assistant running item 为 failed，并 emit `item_completed` + `turn_interrupted`。Chat 面板在 streaming 状态显示 `Stop` 按钮，调用 `interruptCodexTurn`，pending approvals 仍由 reducer 在收到 `turn_interrupted` 时统一清理。
  - Task 17.4 当前验证：`cargo test --manifest-path src-tauri/Cargo.toml services::codex_bridge_service::tests -- --nocapture` 通过 11 个测试；`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过；TypeScript diagnostics 通过；Vite build 通过；`git diff --check` 通过。Vite 使用 Node 20.11.1 构建成功但仍提示低于推荐版本。
  - 当前进度：Task 17.5 Codex thread resume 最小恢复已完成；cached thread 直接返回 runtime 中的 thread；uncached thread 必须提供 `cwd` 和本地 Codex executable 配置 / PATH 可发现 executable，成功后创建 idle runtime 并可通过 `codex_thread_read` 读取空 snapshot。当前阶段不解析 rollout JSONL、不重放历史 items，这部分留给后续历史恢复增强。
  - Task 17.5 当前验证：`cargo test --manifest-path src-tauri/Cargo.toml services::codex_bridge_service::tests -- --nocapture` 通过 14 个测试；`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过；TypeScript diagnostics 通过；Vite build 通过；`git diff --check` 与 `git diff --cached --check` 通过。Vite 使用 Node 20.11.1 构建成功但仍提示低于推荐版本。
  - 当前进度：Task 18 Approval Core 已完成；后端支持从 Codex JSONL 解析 `approval_requested` / `approval_request` 事件，覆盖 command、file change、MCP tool、user input 四类请求；pending approval 写入 runtime snapshot，并通过 `codex://conversation-event` 发出 `approval_requested`；`codex_approval_respond` 注入 `AppHandle` 后可处理 approve / deny，移除 pending approval 并发出 `approval_resolved`。前端 Chat 面板已接入 approve / deny 按钮；command 展示命令和 cwd，file change 展示文件路径和 diff 摘要，user input 支持文本回复与最多 3 个选项。
  - Task 18 当前验证：红灯阶段已确认新测试因缺少 approval helper 失败；实现后 `cargo test --manifest-path src-tauri/Cargo.toml services::codex_bridge_service::tests -- --nocapture` 通过 19 个测试；TypeScript diagnostics 通过。说明：当前仅完成本地 runtime / UI 状态闭环，未声称已验证 Codex CLI 外部进程审批后继续执行。
  - 当前进度：Task 19 MCP Core 与 workspace binding 集成已完成后端闭环；`workspace_service` 新增 Codex MCP projection，按 `enabled_codex` 叠加 workspace override 过滤有效 server；`codex_turn_start` 从 workspace DB 注入本次 turn 的 MCP projection；`codex exec` 通过 `-c mcp_servers.<id>=...` per-run 注入，不回写全局 `~/.codex/config.toml`；`TurnStarted` event 只暴露 `mcpServerCount`、`mcpServerIds`、`mcpServerNames` 安全摘要，不暴露 `env`、`headers`、token 内容。
  - Task 19 当前验证：红灯阶段已确认 `build_codex_mcp_projection` 缺失导致定向测试失败；实现后 `cargo test --manifest-path src-tauri/Cargo.toml services::workspace_service::tests -- --nocapture` 通过 22 个测试，`cargo test --manifest-path src-tauri/Cargo.toml services::codex_bridge_service::tests -- --nocapture` 通过 21 个测试。待继续运行格式、TypeScript、Vite 和 diff 检查。
  - Task 19 收口验证：`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过，仅保留既有 `WorkspaceInput` dead_code warning；TypeScript diagnostics 通过；Vite build 通过，但 Node 20.11.1 低于 Vite 推荐版本提示仍存在；`git diff --check` 与 `git diff --cached --check` 通过，其中 `git diff --check` 仅输出 CRLF 替换提示。
  - 当前进度：Task 20 Terminal Core 已完成静态闭环复核；现有 `WorkspaceActions` 的 `terminalPath` 已优先使用 `selectedWorkspace.rootPath`，并调用 `open_in_terminal`；会话详情恢复按钮继续使用 `SessionMeta.resumeCommand` 调 `launch_resume_session`，`cwd` 来自 `session.projectDir`；未引入 `node-pty` 或内置 terminal。
  - Task 20 当前验证：代码检索确认 `open_in_terminal` 与 `launch_resume_session` 已注册在 `tauri::generate_handler!`；TypeScript diagnostics / Vite build / `cargo check` 已在 Task 19 收口验证中通过。未执行真实打开终端操作，避免在当前自动流程里弹出本地终端窗口。
  - 当前进度：Task 21 Git Core 已完成只读状态闭环；新增 `workspace_git_service` 读取 Git branch、dirty 状态和变更数量，非 Git workspace 返回空状态；新增 `get_workspace_git_status` Tauri command；前端 overview 新增 `WorkspaceGitPanel` 展示 branch、clean / dirty、origin 和 Git root。create branch / push / PR 按计划延后，未加入未实现按钮。
  - Task 21 当前验证：红灯阶段已确认 `get_workspace_git_status` 缺失导致定向测试失败；实现后 `cargo test --manifest-path src-tauri/Cargo.toml services::workspace_git_service::tests -- --nocapture` 通过 3 个测试；`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过，仅保留既有 `WorkspaceInput` dead_code warning；TypeScript diagnostics 通过；Vite build 通过，但 Node 20.11.1 低于 Vite 推荐版本提示仍存在；`git diff --check` 与 `git diff --cached --check` 通过，其中 `git diff --check` 仅输出 CRLF 替换提示。
  - 当前进度：Task 22 Worktree Core 已完成只读预留；新增 `WorkspaceWorktree` 模型、`worktree_service` 解析 `git worktree list --porcelain`、`list_workspace_worktrees` Tauri command，以及前端 overview `WorkspaceWorktreesPanel`。非 Git workspace 返回空列表；cleanup / delete / create worktree 均按计划延后，未加入高风险操作入口。
  - Task 22 当前验证：红灯阶段已确认 `list_workspace_worktrees` 缺失导致定向测试失败；实现后 `cargo test --manifest-path src-tauri/Cargo.toml services::worktree_service::tests -- --nocapture` 通过 3 个测试，`cargo test --manifest-path src-tauri/Cargo.toml models::worktree::tests -- --nocapture` 通过 2 个测试；`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过，仅保留既有 `WorkspaceInput` dead_code warning；TypeScript diagnostics 通过；Vite build 通过，但 Node 20.11.1 低于 Vite 推荐版本提示仍存在；`git diff --check` 与 `git diff --cached --check` 通过，其中 `git diff --check` 仅输出 CRLF 替换提示。
  - 当前进度：Task 23 Local Environment Core 已完成；新增 `LocalEnvironmentConfig` / `LocalEnvironmentUpdateInput`、`local_environment_service`、`read_local_environment` / `save_local_environment` Tauri command，以及前端 `LocalEnvironmentPanel`。面板固定预览 / 编辑 `<workspace>/.codex/environment.toml` 的 setup script，保存路径由后端从 workspace root 计算并校验在 workspace 内；本阶段未执行 setup script，未新增 Run / Execute 入口。
  - Task 23 当前验证：红灯阶段已确认缺少 `models::local_environment`、`read_local_environment`、`save_local_environment` 导致定向测试失败；实现后 `cargo test --manifest-path src-tauri/Cargo.toml services::local_environment_service::tests -- --nocapture` 通过 5 个测试，覆盖缺失文件默认空配置、保存写入 workspace 内、解析错误保留 raw TOML、workspace id mismatch 拒绝、非空 setup script 内容保真。`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过，仅保留既有 `WorkspaceInput` dead_code warning；TypeScript diagnostics 通过；Vite build 通过，但 Node 20.11.1 低于 Vite 推荐版本提示仍存在；workspace i18n 对照通过；`git diff --check` 与 `git diff --cached --check` 通过。
  - 当前进度：Task 24 Automation Core 已完成；新增 `WorkspaceAutomation` / `CreateWorkspaceAutomationInput` / `UpdateWorkspaceAutomationInput`、`workspace_automations` SQLite 表、automation DAO / service / Tauri commands，以及前端 `AutomationPanel`。本阶段只做 workspace automation 配置 CRUD，`schedule` 仅作为字符串保存，`memoryPath` 仅作为可选字符串保存；未接 `$CODEX_HOME/automations`，未写 JSON / TOML 文件，未新增 run-now、后台调度器或 Codex CLI 执行入口。
  - Task 24 当前验证：红灯阶段已确认缺少 `models::automation`、automation DAO 方法和 service 函数导致定向测试失败；实现后 `cargo test --manifest-path src-tauri/Cargo.toml database::dao::automations::tests -- --nocapture` 通过 2 个测试，覆盖 CRUD 排序和 workspace 删除级联；`cargo test --manifest-path src-tauri/Cargo.toml services::automation_service::tests -- --nocapture` 通过 3 个测试，覆盖 create / update / list / delete、缺失 workspace、空 title / prompt / schedule、missing update / delete 明确错误。`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过，仅保留既有 `WorkspaceInput` dead_code warning；TypeScript diagnostics 通过；Vite build 通过，但 Node 20.11.1 低于 Vite 推荐版本提示仍存在；workspace i18n 对照通过；`git diff --check` 与 `git diff --cached --check` 通过。
  - 当前进度：Task 25 动态 capability schema 已完成；新增 `capabilities` / `capability_bindings` SQLite 表、capability model / DAO / service，MCP / skill / prompt 旧启用状态会幂等同步为 app capability binding；不删除旧列，不改变前端数据结构，不接后台调度或执行入口。MCP 列表与 Codex workspace MCP 注入路径已通过 service 应用动态 binding 覆盖；MCP import、配置导入、skill install / import / scan / toggle、prompt upsert / enable / disable / import 也会同步 capability 记录。
  - Task 25 当前验证：红灯阶段已确认 capability DAO API 缺失导致测试失败；实现后 `cargo test --manifest-path src-tauri/Cargo.toml database::dao::capabilities::tests -- --nocapture` 通过 4 个测试；`cargo test --manifest-path src-tauri/Cargo.toml services::capability_service::tests -- --nocapture` 通过 6 个测试；`cargo test --manifest-path src-tauri/Cargo.toml services::mcp_service::tests -- --nocapture` 通过 1 个测试；`cargo test --manifest-path src-tauri/Cargo.toml services::workspace_service::tests -- --nocapture` 通过 22 个测试；`cargo test --manifest-path src-tauri/Cargo.toml services::import_export_service::tests -- --nocapture` 通过 2 个测试；`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过，仅保留既有 `WorkspaceInput` dead_code warning；TypeScript diagnostics 通过；Vite build 通过，但 Node 20.11.1 低于 Vite 推荐版本提示仍存在；安全检索确认 Task 25 未新增外部命令执行 / 后台调度入口；`git diff --check` 与 `git diff --cached --check` 通过。
  - 当前进度：Task 26 前端 registry 驱动化已完成；新增 `useVisibleAppOptions` 作为 registry-first + legacy fallback 的统一前端 app 选项入口，`App.tsx` 启动时预热 adapter registry；Provider / Workspace / Proxy / MCP / Prompt / Skill / Dashboard 相关可见 app tab、label、颜色和默认目标均改为优先来自 registry。MCP / Skill / Prompt 仍保留旧三列兼容映射，只对后端已支持的 `claude / codex / gemini` 输出 toggle，不让 UI 假装支持未落后端字段的新 app。
  - Task 26 当前验证：TypeScript diagnostics 通过；Vite build 通过，但 Node 20.11.1 低于 Vite 推荐版本提示仍存在；`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过，仅保留既有 `WorkspaceInput` dead_code warning；`git diff --check` 与 `git diff --cached --check` 通过，其中 `git diff --check` 仅输出 CRLF 替换提示。
  - Task 27 自动验证：`npm run build` 通过（使用 `C:\Users\Administrator\AppData\Local\nvm\v20.11.1\node.exe` 调用 npm CLI 执行项目脚本）；`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过，仅保留既有 `WorkspaceInput` dead_code warning；`cargo test --manifest-path src-tauri/Cargo.toml` 通过，160 passed，0 failed，doc-tests 2 ignored；`git diff --check` 与 `git diff --cached --check` 通过。
  - Task 27 手动回归：未在当前自动流程中启动 Tauri 桌面应用执行 Provider 导入 / 导出、MCP 启用 / 禁用、Workspaces 历史会话读取、Dashboard 项目扫描的人工点击回归；进入发布前仍需在 `npm run tauri dev` 或安装包中完成该项。
  - 当前状态：Task 1-27 的代码实现、前端构建、Rust 编译、Rust 全量测试和 diff 检查均已完成；剩余风险集中在 Task 27.4 桌面 UI 手动回归。
- [ ] 第二批：CodexBridge + Chat UI + Approval + MCP 注入。
  - 验证方式：真实 Codex thread / turn 可在选定 workspace 内启动、流式展示、审批、终止和恢复。
  - 当前状态：等待第一批通过后执行。
- [ ] 第三批：Terminal / Git / Worktree / Local Environment / Automation / 动态 capability schema。
  - 验证方式：每个增强能力单独确认、单独实现、单独验证。
  - 当前状态：暂缓，不进入首批实现。
